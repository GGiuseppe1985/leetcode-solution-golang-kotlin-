func maximumElementAfterDecrementingAndRearranging(arr []int) int {
    sort.Ints(arr)
    res := 1
    for i := 1;i < len(arr); i++ {
        if arr[i] > result {
            res++
        }
    }
    return res
}
