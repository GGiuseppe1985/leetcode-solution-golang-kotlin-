func averageValue(nums []int) int {
    var sum, count int
    for _, num := range nums {
        if num % 6 == 0{
            sum += num
            count++
        }
    }
    if count == 0{
        return 0
    } else {
        return sum/count
    }
}
