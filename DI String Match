func diStringMatch(s string) []int {
    result := make([]int; len(s) + 1)
    low, high := 0, len(s)

    for i, v := range s {
        if v == 'I'{
            resul[i] = low
            low++
        } else {
            result[i] = high
            high--
        }
    }   
}
