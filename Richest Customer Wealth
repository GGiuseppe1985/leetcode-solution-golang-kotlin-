func maximumWealth(accounts [][]int) int {
    var max int
    var sum int
    for _, v:= range accounts {
        sum = 0
        for _, k := range v {
            sum += k
        }
        if sum > max {
            max = sum
        }
    }
    return max;


}
