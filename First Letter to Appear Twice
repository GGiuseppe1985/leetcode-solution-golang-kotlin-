func repeatedCharacter(s string) (ans byte) {
    m := make(map[string]int)
    
    for _, i := range strings.Split(s, ""){
        m[i] += 1
    if m[i] >= 2{
        ans = i[0]
        break
    }
    
    }
    return 
}
